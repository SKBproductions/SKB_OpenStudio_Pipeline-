# Workflow Documentation

This directory is reserved for safe workflow documentation and future non-secret automation examples.

Workflow files may refer to environment-variable names such as `RUNWAYML_API_SECRET`, but must never contain real credentials. Paid generation workflows should preserve manual approval checkpoints.
